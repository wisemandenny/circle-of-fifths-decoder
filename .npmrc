# Use public npm registry (override global Artifactory/corporate registry)
registry=https://registry.npmjs.org/

# Prevent install from hanging on slow or flaky networks
fetch-timeout=60000
fetch-retries=2
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# Prefer smaller, faster installs
prefer-offline=true
progress=true
